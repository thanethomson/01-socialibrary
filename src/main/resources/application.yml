spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    platform: postgres
    url: jdbc:postgresql://192.168.99.100:5432/socialibrary
    username: socialibrary
    password: socialibrary
    driver-class-name: org.postgresql.Driver

server:
  port: 8080
